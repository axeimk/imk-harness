# ハーネス採否の記録

汎用ハーネスの提案（verify スキル、hooks、CONTEXT.md など）の採否を記録する。
「使わない」と記録された項目をエージェントは再提案しない。

- 使用ツール: <Claude Code / Codex / Cursor のうちこのプロジェクトで使うもの（<日付> 確認）>
- CLAUDE.md / AGENTS.md（「作業の進め方」節を含む）: <作成済み / 使わない（<日付> ユーザー判断）>
- verify スキル: <使う / 使わない（<日付> ユーザー判断）>
- tdd スキル: <使う / 使わない（<日付> ユーザー判断）>
- domain-modeling スキル: <導入済み / 使わない（<日付> ユーザー判断）>
- grilling スキル: <導入済み / 使わない（<日付> ユーザー判断）>
- CONTEXT.md（用語集）: <導入済み / 使わない（<日付> ユーザー判断）>
- ADR（設計記録）: <導入済み（`docs/adr/`） / 使わない（<日付> ユーザー判断）>
- permissions / hooks: <導入済み（<設定場所>） / 使わない>
